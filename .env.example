# Example environment variables for garden-api (DO NOT commit real secrets)
JWT_SECRET=
DEMO_USER=username
DEMO_PASS=password
PORT=3000

# Optional: set a path for SQLite if you mount a volume on deployment
# DATA_DIR=/data
# DB_PATH=${DATA_DIR:-.}/garden.db
